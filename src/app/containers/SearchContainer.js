import React from 'react';
import { Text, View } from 'react-native';

const SearchContainer = () => (
  <View>
    <Text>Welcome to SearchContainer</Text>
  </View>
);

export default SearchContainer;
